# คู่มือการทดสอบระบบ - Testing Guide

## ✅ ปัญหาที่แก้ไขแล้ว (Fixed Issues)

### 1. CONFIG is not defined Error
**ปัญหา:** เมื่อรันเทส หรือเพิ่มเครื่อง PC ระบบเกิด error `CONFIG is not defined`

**สาเหตุ:** Browser cache เก็บไฟล์ config.js เวอร์ชันเก่าที่ไม่มี properties:
- `MAX_SESSION_TIME`
- `WARNING_TIME`
- `SOFTWARE` (in STORAGE_KEYS)

**วิธีแก้ไข:** ✅ เพิ่ม cache-busting parameters (`?v=2`) ใน script tags ทุกหน้า

**ไฟล์ที่แก้ไข:**
- ✅ simple-test.html
- ✅ run-tests.html
- ✅ prototype/test-add-pc.html
- ✅ prototype/admin/manage-pc.html

---

## 🧪 วิธีทดสอบหลังแก้ไข

### ขั้นตอนที่ 1: Hard Refresh Browser
เพื่อให้แน่ใจว่า browser โหลดไฟล์ใหม่:

**วิธีที่ 1: Hard Refresh**
- Windows/Linux: `Ctrl + F5` หรือ `Ctrl + Shift + R`
- Mac: `Cmd + Shift + R`

**วิธีที่ 2: Clear Cache**
1. เปิด DevTools (F12)
2. คลิกขวาที่ปุ่ม Refresh
3. เลือก "Empty Cache and Hard Reload"

### ขั้นตอนที่ 2: ทดสอบด้วย simple-test.html

**วิธีทดสอบ:**
```
1. เปิด file:///E:/FYPrj/checkinSys/simple-test.html
2. ดูผลลัพธ์เมื่อโหลดหน้า:
   ✅ DataManager loaded
   ✅ CONFIG loaded
   ✅ utils.js loaded
```

**ถ้าเห็นข้อความข้างต้น แสดงว่าแก้ไขสำเร็จ! ✅**

**ทดสอบเพิ่มเติม:**
1. คลิก "Test 1: Check DataManager" → ต้องได้ ✅ Test 1 PASSED
2. คลิก "Test 2: Add PC" → ต้องได้ ✅ Test 2 PASSED
3. คลิก "Test 3: List All PCs" → ต้องเห็นรายการ PC
4. คลิก "Test 4: Delete Test PC" → ต้องลบ PC ทดสอบสำเร็จ
5. คลิก "Test 5: Full Workflow" → ต้องผ่านทุกขั้นตอน

---

### ขั้นตอนที่ 3: ทดสอบด้วย manage-pc.html

**วิธีทดสอบ:**
```
1. เปิด file:///E:/FYPrj/checkinSys/prototype/admin/manage-pc.html
2. คลิก "+ เพิ่มเครื่องใหม่"
3. กรอกข้อมูล:
   - PC ID: PC-999
   - Location: Z99
   - Status: available
4. คลิก "บันทึก"
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ Modal ปิด
- ✅ แสดงข้อความ "เพิ่มเครื่อง PC-999 สำเร็จ"
- ✅ ตารางแสดง PC-999
- ✅ Refresh หน้าแล้ว PC-999 ยังคงอยู่

---

### ขั้นตอนที่ 4: ทดสอบด้วย Automated Tests

**วิธีทดสอบ:**
```
1. เปิด file:///E:/FYPrj/checkinSys/run-tests.html
2. คลิก "▶️ Run All Tests"
3. รอดูผลลัพธ์
```

**ผลลัพธ์ที่คาดหวัง:**
```
📊 Test Summary
Total: 5
✅ Passed: 5
❌ Failed: 0
🎉 All tests passed!
```

---

## 🔍 การตรวจสอบ Console

### เปิด Developer Console (F12) และตรวจสอบ:

**✅ ต้องเห็น:**
```javascript
✅ Test page loaded
📦 DataManager available: object
✅ DataManager loaded
✅ CONFIG loaded
✅ utils.js loaded
```

**❌ ไม่ควรเห็น:**
```javascript
❌ CONFIG is not defined
❌ DataManager is not defined
❌ getComputers is not defined
```

---

## 🔧 ตรวจสอบ LocalStorage

### วิธีตรวจสอบ:
1. เปิด DevTools (F12)
2. ไปที่ Application Tab → Local Storage → `file://`
3. ตรวจสอบ keys:

**✅ ต้องมี:**
- `checkin_computers` → array ของ PC ทั้งหมด
- `checkin_usage_logs` → array ของ usage logs
- `checkin_external_users` → array ของผู้ใช้ภายนอก
- `checkin_reservations` → array ของการจอง

**ตัวอย่าง checkin_computers:**
```json
[
  {
    "id": "PC-001",
    "row": "A",
    "number": 1,
    "location": "A1",
    "status": "available",
    "specs": "i5-11400, 16GB RAM, 512GB SSD",
    "software": ["MS Office", "VS Code", "Chrome"],
    "createdAt": "2024-11-28T...",
    "updatedAt": "2024-11-28T..."
  }
]
```

---

## 📊 Test Checklist

### เทสต์พื้นฐาน (Basic Tests)
- [ ] เปิด simple-test.html ได้โดยไม่มี error
- [ ] แสดงข้อความ "✅ DataManager loaded"
- [ ] แสดงข้อความ "✅ CONFIG loaded"
- [ ] Test 1 ผ่าน (Check DataManager)
- [ ] Test 2 ผ่าน (Add PC)
- [ ] Test 3 ผ่าน (List All PCs)
- [ ] Test 4 ผ่าน (Delete Test PC)
- [ ] Test 5 ผ่าน (Full Workflow)

### เทสต์การเพิ่ม PC (Add PC Tests)
- [ ] เปิด manage-pc.html ได้
- [ ] เพิ่ม PC ใหม่สำเร็จ
- [ ] แสดงข้อความสำเร็จ
- [ ] PC ใหม่ปรากฏในตาราง
- [ ] Refresh แล้ว PC ยังคงอยู่
- [ ] ดูใน LocalStorage แล้วพบ PC ใหม่

### เทสต์ Automated (Automated Tests)
- [ ] เปิด run-tests.html ได้
- [ ] คลิก "Run All Tests" แล้วทำงาน
- [ ] ทุกเทสต์ผ่าน (5/5)
- [ ] ไม่มี error ใน Console

---

## 🐛 การแก้ไขปัญหาเพิ่มเติม (Troubleshooting)

### ถ้ายังเห็น "CONFIG is not defined"

**ขั้นตอนแก้ไข:**

1. **Clear Browser Cache อีกครั้ง**
   ```
   - เปิด DevTools (F12)
   - Application → Storage → Clear site data
   - ปิดหน้าเบราว์เซอร์
   - เปิดใหม่อีกครั้ง
   ```

2. **ตรวจสอบว่าไฟล์ถูกโหลด**
   ```
   - เปิด DevTools (F12)
   - ไปที่ Network Tab
   - Refresh หน้า (F5)
   - ตรวจสอบว่า config.js?v=2 ถูกโหลด (status 200)
   - คลิกดูเนื้อหาของ config.js
   - ต้องเห็น MAX_SESSION_TIME: 120 บรรทัดที่ 248
   ```

3. **ใช้ Incognito/Private Mode**
   ```
   - เปิด Incognito Window (Ctrl + Shift + N)
   - เปิดหน้าทดสอบใหม่
   - ทดสอบอีกครั้ง
   ```

### ถ้า Test 2 ยังไม่ผ่าน

**ขั้นตอนแก้ไข:**

1. **ดู Console Log**
   ```javascript
   // ต้องเห็น:
   [DataManager] addComputer called with: {...}
   [DataManager] Current computers count: X
   [DataManager] Creating new computer: {...}
   [DataManager] Computer saved. New count: X+1
   ```

2. **ตรวจสอบ LocalStorage**
   ```
   - F12 → Application → Local Storage
   - ดู checkin_computers
   - ตรวจสอบว่ามี PC ใหม่
   ```

3. **ลอง Clear LocalStorage และทดสอบใหม่**
   ```javascript
   // ใน Console:
   localStorage.clear();
   location.reload();
   ```

### ถ้า manage-pc.html ยังเพิ่ม PC ไม่ได้

**ขั้นตอนแก้ไข:**

1. **ตรวจสอบ Console Errors**
   ```
   - F12 → Console
   - ดูว่ามี error อะไรเมื่อคลิก "บันทึก"
   - ถ้ามี error ให้แจ้งข้อความ error
   ```

2. **ทดสอบด้วย test-add-pc.html แทน**
   ```
   - เปิด file:///E:/FYPrj/checkinSys/prototype/test-add-pc.html
   - กรอก PC ID และ Location
   - คลิก "Add Computer"
   - ดูผลลัพธ์
   ```

---

## 📝 สรุปการแก้ไข (Summary)

### ปัญหาหลัก:
Browser cache ทำให้ config.js เวอร์ชันเก่าถูกใช้งาน ซึ่งไม่มี properties ที่จำเป็น

### วิธีแก้:
เพิ่ม cache-busting parameters (`?v=2`) ให้กับ script tags ทั้งหมด

### ไฟล์ที่มีการเปลี่ยนแปลง:
1. **config.js** (บรรทัด 244-259) - เพิ่ม MAX_SESSION_TIME, WARNING_TIME, SOFTWARE
2. **simple-test.html** - เพิ่ม ?v=2 ให้ทุก script
3. **run-tests.html** - เพิ่ม ?v=2 ให้ทุก script
4. **prototype/test-add-pc.html** - เพิ่ม ?v=2 ให้ทุก script
5. **prototype/admin/manage-pc.html** - เพิ่ม ?v=2 ให้ทุก script

### การทดสอบ:
- ✅ Hard refresh browser (Ctrl + F5)
- ✅ ทดสอบด้วย simple-test.html
- ✅ ทดสอบด้วย run-tests.html
- ✅ ทดสอบด้วย manage-pc.html

---

## 📞 ช่วยเหลือเพิ่มเติม

หากยังมีปัญหา กรุณาส่งข้อมูลต่อไปนี้:
1. Screenshot ของ Console (F12)
2. Screenshot ของ Application → Local Storage
3. Screenshot ของ Network Tab (แสดงไฟล์ที่โหลด)
4. ข้อความ error ที่เกิดขึ้น (ถ้ามี)

---

**อัพเดทล่าสุด:** 2024-11-28
**เวอร์ชัน:** v2 (with cache-busting)
