# 🌐 คู่มือการใช้งาน ngrok กับระบบ Lab Check-in

## 📋 ข้อกำหนดเบื้องต้น

1. **Docker** ต้องรันอยู่
2. **ngrok** ติดตั้งแล้ว (ดาวน์โหลดจาก https://ngrok.com/download)
3. **ngrok account** (สมัครฟรีที่ https://dashboard.ngrok.com/signup)

---

## 🚀 วิธีการใช้งาน ngrok

### ขั้นตอนที่ 1: เริ่มต้น Docker Container

```bash
cd lab_system
docker-compose up -d
```

ตรวจสอบว่า container รันอยู่:
```bash
docker-compose ps
```

คุณควรเห็น:
- ✅ lab_system-web-1 (port 8000)
- ✅ lab_system-db-1 (port 3306)
- ✅ lab_system-phpmyadmin-1 (port 8080)

---

### ขั้นตอนที่ 2: เชื่อมต่อ ngrok account (ครั้งแรกเท่านั้น)

1. ไปที่ https://dashboard.ngrok.com/get-started/your-authtoken
2. คัดลอก authtoken ของคุณ
3. รันคำสั่ง:

```bash
ngrok config add-authtoken YOUR_AUTHTOKEN_HERE
```

---

### ขั้นตอนที่ 3: เริ่มต้น ngrok Tunnel

เปิด **terminal ใหม่** แล้วรันคำสั่งนี้:

```bash
ngrok http 8000
```

หรือถ้าต้องการ custom domain (ngrok paid plan):
```bash
ngrok http 8000 --domain=your-custom-domain.ngrok-free.app
```

---

### ขั้นตอนที่ 4: ดู URL ที่ ngrok สร้างให้

หลังจากรันคำสั่ง ngrok คุณจะเห็นหน้าจอประมาณนี้:

```
ngrok

Session Status                online
Account                       Your Name (Plan: Free)
Version                       3.x.x
Region                        Asia Pacific (ap)
Latency                       -
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc-def-ghi.ngrok-free.app -> http://localhost:8000

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**สำคัญ:** คัดลอก URL ในบรรทัด `Forwarding` (ตัวอย่าง: `https://abc-def-ghi.ngrok-free.app`)

---

### ขั้นตอนที่ 5: เข้าถึงระบบผ่าน ngrok

เปิดเบราว์เซอร์และไปที่ URL ที่ ngrok สร้างให้:

```
https://abc-def-ghi.ngrok-free.app
```

**หมายเหตุ:** ngrok Free plan จะแสดงหน้า warning ก่อน ให้กด **"Visit Site"** เพื่อเข้าสู่ระบบ

---

## 🔗 URL ที่สามารถเข้าถึงได้ผ่าน ngrok

แทนที่ `YOUR_NGROK_URL` ด้วย URL จริงที่ ngrok สร้างให้:

| หน้า | URL |
|------|-----|
| หน้าแรก (Kiosk) | `https://YOUR_NGROK_URL/` |
| Dashboard | `https://YOUR_NGROK_URL/manager/dashboard/` |
| จัดการเครื่อง | `https://YOUR_NGROK_URL/manager/manage/` |
| จัดการซอฟต์แวร์ | `https://YOUR_NGROK_URL/manager/software/` |
| รายงานสถิติ | `https://YOUR_NGROK_URL/manager/report/` |
| Django Admin | `https://YOUR_NGROK_URL/admin/` |

**Login Credentials:**
- Username: `admin`
- Password: `admin123`

---

## 🛠️ การตั้งค่าที่ได้ทำไว้แล้ว

ไฟล์ `settings.py` ได้ถูกตั้งค่าให้รองรับ ngrok แล้ว:

```python
# อนุญาตทุก host
ALLOWED_HOSTS = ['*']

# อนุญาต CSRF สำหรับ ngrok domains
CSRF_TRUSTED_ORIGINS = [
    'https://*.ngrok-free.app',
    'https://*.ngrok-free.dev',
    'https://*.ngrok.io',
    'http://localhost:8000',
    'http://127.0.0.1:8000',
]
```

**คุณไม่ต้องแก้ไขอะไรเพิ่มเติม!** ระบบพร้อมใช้งานกับ ngrok URL ใดๆ แล้ว

---

## 📱 การทดสอบบนมือถือ

1. เปิด ngrok tunnel ตามขั้นตอนข้างบน
2. เปิดเบราว์เซอร์บนมือถือ
3. เข้า URL ที่ ngrok สร้างให้
4. กด "Visit Site" (ถ้ามี warning)
5. ใช้งานระบบได้เลย!

---

## ⚠️ ข้อควรระวัง

### 1. ngrok Free Plan Limitations:
- URL จะเปลี่ยนทุกครั้งที่ restart ngrok
- Session timeout หลัง 2 ชั่วโมง (ต้อง restart)
- มีหน้า warning ก่อนเข้าเว็บ
- จำกัด 40 connections/minute

### 2. Security:
- **อย่าแชร์ URL ในที่สาธารณะ** เนื่องจากใครก็ได้สามารถเข้าถึงได้
- URL ของ ngrok เป็น HTTPS อัตโนมัติ (ปลอดภัย)
- ข้อมูลใน database จะไม่หายแม้ restart ngrok

### 3. Performance:
- ความเร็วขึ้นอยู่กับ ngrok region (แนะนำ Asia Pacific)
- Latency จะสูงกว่า localhost

---

## 🔧 คำสั่งที่มีประโยชน์

### ดู ngrok Web Interface (ดู traffic realtime):
```
http://127.0.0.1:4040
```

### เลือก region ที่ใกล้ที่สุด:
```bash
ngrok http 8000 --region ap  # Asia Pacific
ngrok http 8000 --region us  # United States
ngrok http 8000 --region eu  # Europe
```

### Restart ngrok:
1. กด `Ctrl+C` ใน terminal ที่รัน ngrok
2. รันคำสั่ง `ngrok http 8000` ใหม่อีกครั้ง
3. **หมายเหตุ:** URL จะเปลี่ยนใหม่

### หยุด Docker:
```bash
cd lab_system
docker-compose down
```

### Restart Django (ถ้าแก้ไขโค้ด):
```bash
cd lab_system
docker-compose restart web
```

---

## 🎯 ตัวอย่างการใช้งานจริง

### Scenario 1: แชร์ให้เพื่อนทดสอบ
```bash
# Terminal 1: Start Docker
cd lab_system
docker-compose up -d

# Terminal 2: Start ngrok
ngrok http 8000

# แชร์ URL ให้เพื่อน เช่น:
# https://abc-123-xyz.ngrok-free.app
```

### Scenario 2: ทดสอบบนมือถือ
```bash
# เปิด ngrok
ngrok http 8000

# สแกน QR Code ที่แสดงใน terminal
# หรือพิมพ์ URL ในมือถือ
```

### Scenario 3: Demo ให้อาจารย์/ผู้ดูแล
```bash
# เปิด ngrok ก่อนนัดหมาย 10 นาที
ngrok http 8000

# บันทึก URL ไว้
# Demo ผ่าน URL นั้น
```

---

## 📞 การแก้ปัญหา

### ปัญหา: ngrok แสดง "ERR_NGROK_3200"
**แก้:**
```bash
# Restart ngrok
# กด Ctrl+C แล้วรันใหม่
ngrok http 8000
```

### ปัญหา: หน้าเว็บแสดง "CSRF verification failed"
**แก้:**
1. ตรวจสอบว่า URL เริ่มด้วย `https://` (ไม่ใช่ http)
2. ลองล้าง cache และ cookies
3. ตรวจสอบ `settings.py` ว่ามี CSRF_TRUSTED_ORIGINS ครบ

### ปัญหา: Docker ไม่รัน
**แก้:**
```bash
cd lab_system
docker-compose down
docker-compose up -d
docker-compose logs web
```

### ปัญหา: ngrok หยุดทำงานเอง
**แก้:**
- Free plan มี session timeout 2 ชั่วโมง
- Restart ngrok ใหม่
- พิจารณาอัพเกรด plan ถ้าใช้งานบ่อย

---

## 🎓 Tips สำหรับการใช้งาน

1. **บุ๊กมาร์ก ngrok dashboard:** https://dashboard.ngrok.com/
2. **ใช้ ngrok config file** สำหรับ custom settings
3. **อัพเกรด ngrok plan** ถ้าต้องการ custom domain ที่ไม่เปลี่ยน
4. **ติดตาม traffic** ผ่าน Web Interface (http://127.0.0.1:4040)
5. **ใช้ ngrok TCP tunnel** ถ้าต้องการเข้าถึง MySQL โดยตรง:
   ```bash
   ngrok tcp 3306
   ```

---

## ✅ Checklist ก่อนแชร์ URL

- [ ] Docker containers รันอยู่ (`docker-compose ps`)
- [ ] ngrok tunnel เปิดอยู่
- [ ] ทดสอบเข้า URL ด้วยตัวเองก่อน
- [ ] Login admin ได้ (admin/admin123)
- [ ] ทดสอบฟีเจอร์หลักๆ ทำงานปกติ
- [ ] เตรียม URL และ credentials ไว้แชร์

---

## 📚 เอกสารเพิ่มเติม

- ngrok Documentation: https://ngrok.com/docs
- Django CSRF: https://docs.djangoproject.com/en/5.0/ref/csrf/
- Docker Compose: https://docs.docker.com/compose/

---

**สร้างโดย:** Claude Code
**วันที่:** 29 พฤศจิกายน 2025
**เวอร์ชัน:** 1.0
