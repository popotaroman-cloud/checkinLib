# 🖥️ Dashboard In-Use Management - Lab Check-in System

## Overview

อัปเดตหน้า Dashboard ให้สามารถจัดการเครื่องที่มีสถานะ **In Use** และ **Maintenance** ได้อย่างยืดหยุ่น

---

## ✨ Features ที่เพิ่มเข้ามา

### 1. **จัดการเครื่องที่ใช้งานอยู่ (In Use)**
เมื่อคลิกเครื่องที่มีสถานะ `in_use` จะแสดงเมนู 2 ตัวเลือก:
- **Checkout** - ทำการ checkout ผู้ใช้งานออก และเปลี่ยนสถานะเป็น `available`
- **ส่งซ่อม (Maintenance)** - ทำการ checkout ผู้ใช้งานออก และเปลี่ยนสถานะเป็น `maintenance`

### 2. **เปิดใช้งานเครื่องซ่อม (Maintenance → Available)**
เมื่อคลิกเครื่องที่มีสถานะ `maintenance` จะเปลี่ยนสถานะเป็น `available` ทันที

### 3. **รองรับสถานะ Legacy (Disabled → Available)**
เมื่อคลิกเครื่องที่มีสถานะ `disabled` (เก่า) จะเปลี่ยนสถานะเป็น `available`

### 4. **คงพฤติกรรมเดิมของเครื่องว่าง (Available)**
เมื่อคลิกเครื่องที่มีสถานะ `available` จะต้องกรอกรหัสเจ้าหน้าที่เพื่อเปลี่ยนเป็น `in_use` (เหมือนเดิม)

---

## 📁 Files Modified

### 1. `templates/manager/dashboard.html` - JavaScript Functions

**Changes Made**:

#### 1.1 อัปเดตฟังก์ชัน confirmToggle() (Lines 125-198)

เพิ่มเงื่อนไขสำหรับ `in_use` และ `maintenance`:

```javascript
function confirmToggle(pcId, currentStatus) {
    let newStatus = '';
    let action = '';
    let confirmBtnText = '';
    let confirmBtnColor = '';

    // กรณีเครื่องซ่อม (maintenance) → เปลี่ยนเป็น available
    if (currentStatus === 'maintenance') {
        newStatus = 'available';
        action = 'เปิดใช้งาน (Available)';
        confirmBtnText = 'เปิดใช้งาน';
        confirmBtnColor = '#198754';
    }
    // กรณีเครื่องว่าง (available) → ให้เจ้าหน้าที่เข้าใช้
    else if (currentStatus === 'available') {
        newStatus = 'in_use';
        action = 'เปลี่ยนเป็น "ใช้งาน"';
        confirmBtnText = 'เปลี่ยนสถานะ';
        confirmBtnColor = '#0d6efd';
    }
    // กรณีเครื่องใช้งานอยู่ (in_use) → แสดงเมนูเลือก Checkout หรือ Maintenance
    else if (currentStatus === 'in_use') {
        showInUseMenu(pcId);
        return;
    }
    // กรณี disabled (legacy) → เปิดใช้งาน
    else if (currentStatus === 'disabled') {
        newStatus = 'available';
        action = 'เปิดใช้งาน (Available)';
        confirmBtnText = 'เปิดใช้งาน';
        confirmBtnColor = '#198754';
    }
    else {
        newStatus = 'maintenance';
        action = 'ปิดปรับปรุง (Maintenance)';
        confirmBtnText = 'ปิดเครื่อง';
        confirmBtnColor = '#d33';
    }

    // ... (rest of the function)
}
```

#### 1.2 เพิ่มฟังก์ชัน showInUseMenu() (Lines 200-223)

แสดงเมนูเลือกสำหรับเครื่องที่ใช้งานอยู่:

```javascript
function showInUseMenu(pcId) {
    Swal.fire({
        title: `จัดการเครื่อง ${pcId}`,
        text: 'เครื่องนี้กำลังใช้งานอยู่ คุณต้องการทำอะไร?',
        icon: 'question',
        showDenyButton: true,
        showCancelButton: true,
        confirmButtonText: '<i class="fas fa-sign-out-alt"></i> Checkout',
        confirmButtonColor: '#dc3545',
        denyButtonText: '<i class="fas fa-tools"></i> ส่งซ่อม',
        denyButtonColor: '#6c757d',
        cancelButtonText: 'ยกเลิก',
        cancelButtonColor: '#3085d6'
    }).then((result) => {
        if (result.isConfirmed) {
            // ผู้ใช้เลือก Checkout
            confirmCheckout(pcId);
        } else if (result.isDenied) {
            // ผู้ใช้เลือก Maintenance
            confirmMaintenance(pcId);
        }
    });
}
```

**Key Points**:
- ใช้ `showDenyButton: true` เพื่อแสดง 3 ปุ่ม (Confirm, Deny, Cancel)
- ปุ่ม Checkout (สีแดง) - ยืนยันการ checkout
- ปุ่มส่งซ่อม (สีเทา) - ส่งเครื่องเข้าซ่อม
- ปุ่มยกเลิก (สีน้ำเงิน) - ยกเลิกการดำเนินการ

#### 1.3 เพิ่มฟังก์ชัน confirmCheckout() (Lines 225-242)

ยืนยันการ Checkout:

```javascript
function confirmCheckout(pcId) {
    Swal.fire({
        title: 'ยืนยันการ Checkout',
        text: `คุณต้องการทำการ Checkout ผู้ใช้งานออกจากเครื่อง ${pcId} ใช่หรือไม่?`,
        icon: 'warning',
        showCancelButton: true,
        confirmButtonColor: '#dc3545',
        cancelButtonColor: '#6c757d',
        confirmButtonText: 'ยืนยัน Checkout',
        cancelButtonText: 'ยกเลิก'
    }).then((result) => {
        if (result.isConfirmed) {
            // เปลี่ยนสถานะเป็น available พร้อม force checkout
            updateComputerStatus(pcId, 'available', true, null);
        }
    });
}
```

**Parameters**:
- `newStatus`: `'available'`
- `forceCheckout`: `true` (บังคับ checkout ผู้ใช้งานปัจจุบัน)
- `staffId`: `null` (ไม่ต้องระบุเจ้าหน้าที่)

#### 1.4 เพิ่มฟังก์ชัน confirmMaintenance() (Lines 244-261)

ยืนยันการส่งซ่อม:

```javascript
function confirmMaintenance(pcId) {
    Swal.fire({
        title: 'ยืนยันส่งซ่อม',
        html: `คุณต้องการส่งเครื่อง ${pcId} เข้าซ่อม ใช่หรือไม่?<br><br><strong>หมายเหตุ:</strong> ระบบจะทำการ Checkout ผู้ใช้งานปัจจุบันออกอัตโนมัติ`,
        icon: 'warning',
        showCancelButton: true,
        confirmButtonColor: '#6c757d',
        cancelButtonColor: '#3085d6',
        confirmButtonText: 'ยืนยันส่งซ่อม',
        cancelButtonText: 'ยกเลิก'
    }).then((result) => {
        if (result.isConfirmed) {
            // เปลี่ยนสถานะเป็น maintenance พร้อม force checkout
            updateComputerStatus(pcId, 'maintenance', true, null);
        }
    });
}
```

**Parameters**:
- `newStatus`: `'maintenance'`
- `forceCheckout`: `true` (บังคับ checkout ผู้ใช้งานปัจจุบัน)
- `staffId`: `null` (ไม่ต้องระบุเจ้าหน้าที่)

#### 1.5 อัปเดต Template HTML (Lines 82-91)

เพิ่มการแสดงสถานะ `maintenance`:

```html
{% if pc.status_info.status == 'in_use' %}
    <small>{{ pc.status_info.current_user_name }}</small>
    <small style="color: var(--text-muted);">{{ pc.status_info.start_time|time:"H:i" }} น.</small>
{% elif pc.status_info.status == 'maintenance' %}
    <small>ส่งซ่อม</small>
{% elif pc.status_info.status == 'disabled' %}
    <small>งดให้บริการ</small>
{% else %}
    <small>ว่าง</small>
{% endif %}
```

---

## 🎯 User Flow Diagrams

### Flow 1: คลิกเครื่องที่ใช้งานอยู่ (In Use)

```
User คลิกเครื่อง PC-01 (status: in_use)
    ↓
confirmToggle('PC-01', 'in_use')
    ↓
showInUseMenu('PC-01')
    ↓
แสดง Dialog: "จัดการเครื่อง PC-01"
    ├─ [Checkout] ──→ confirmCheckout('PC-01')
    │                     ↓
    │                 "ยืนยันการ Checkout?"
    │                     ↓
    │                 updateComputerStatus('PC-01', 'available', true, null)
    │                     ↓
    │                 API: /api/update-computer/PC-01/
    │                     ↓
    │                 Status: available, Force Checkout: true
    │                     ↓
    │                 ✅ Checkout สำเร็จ → Reload
    │
    ├─ [ส่งซ่อม] ──→ confirmMaintenance('PC-01')
    │                     ↓
    │                 "ยืนยันส่งซ่อม?"
    │                     ↓
    │                 updateComputerStatus('PC-01', 'maintenance', true, null)
    │                     ↓
    │                 API: /api/update-computer/PC-01/
    │                     ↓
    │                 Status: maintenance, Force Checkout: true
    │                     ↓
    │                 ✅ ส่งซ่อมสำเร็จ → Reload
    │
    └─ [ยกเลิก] ──→ ปิด Dialog
```

### Flow 2: คลิกเครื่องที่ซ่อม (Maintenance)

```
User คลิกเครื่อง PC-02 (status: maintenance)
    ↓
confirmToggle('PC-02', 'maintenance')
    ↓
แสดง Dialog: "จัดการเครื่อง PC-02?"
"คุณต้องการ 'เปิดใช้งาน (Available)' ใช่หรือไม่?"
    ↓
User กด "เปิดใช้งาน"
    ↓
updateComputerStatus('PC-02', 'available', false, null)
    ↓
API: /api/update-computer/PC-02/
    ↓
Status: available
    ↓
✅ เปิดใช้งานสำเร็จ → Reload
```

### Flow 3: คลิกเครื่องว่าง (Available) - เหมือนเดิม

```
User คลิกเครื่อง PC-03 (status: available)
    ↓
confirmToggle('PC-03', 'available')
    ↓
แสดง Dialog: "กรุณากรอกรหัสเจ้าหน้าที่"
    ↓
User กรอก Staff ID
    ↓
updateComputerStatus('PC-03', 'in_use', false, staffId)
    ↓
API: /api/update-computer/PC-03/
    ↓
Status: in_use, Staff ID: xxxxxxx
    ↓
✅ เข้าใช้งานสำเร็จ → Reload
```

---

## 🎨 UI Changes

### Before (เครื่องใช้งานอยู่)
```
┌────────────────────┐
│   🖥️ PC-01         │
│   นายสมชาย ใจดี     │
│   09:00 น.         │
└────────────────────┘
       ↓ (คลิก)
   ไม่มีปุ่มอะไร
```

### After (เครื่องใช้งานอยู่)
```
┌────────────────────┐
│   🖥️ PC-01         │
│   นายสมชาย ใจดี     │
│   09:00 น.         │
└────────────────────┘
       ↓ (คลิก)
┌──────────────────────────┐
│ จัดการเครื่อง PC-01      │
│ เครื่องนี้กำลังใช้งานอยู่ │
│                          │
│  [⮜ Checkout] (สีแดง)   │
│  [🔧 ส่งซ่อม] (สีเทา)   │
│  [ยกเลิก] (สีน้ำเงิน)    │
└──────────────────────────┘
```

### Before (เครื่องซ่อม)
```
┌────────────────────┐
│   🖥️ PC-02         │
│   งดให้บริการ       │
└────────────────────┘
       ↓ (คลิก)
   ไม่มีฟังก์ชันชัดเจน
```

### After (เครื่องซ่อม)
```
┌────────────────────┐
│   🖥️ PC-02         │
│   ส่งซ่อม          │
└────────────────────┘
       ↓ (คลิก)
┌──────────────────────────┐
│ จัดการเครื่อง PC-02?     │
│ คุณต้องการ               │
│ "เปิดใช้งาน (Available)" │
│ ใช่หรือไม่?               │
│                          │
│  [เปิดใช้งาน] (สีเขียว)  │
│  [ยกเลิก]                │
└──────────────────────────┘
```

---

## 🧪 Test Cases

### Test Case 1: Checkout จากเครื่องที่ใช้งานอยู่

**Steps**:
1. เข้าหน้า Dashboard (`/manager/dashboard/`)
2. มีเครื่อง PC-01 สถานะ `in_use` (มีผู้ใช้งาน)
3. คลิกที่เครื่อง PC-01
4. เลือกปุ่ม "Checkout" (สีแดง)
5. กด "ยืนยัน Checkout"

**Expected**:
- แสดงข้อความยืนยัน "คุณต้องการทำการ Checkout ผู้ใช้งานออกจากเครื่อง PC-01 ใช่หรือไม่?"
- หลังกดยืนยัน:
  - เครื่อง PC-01 เปลี่ยนสถานะเป็น `available`
  - บันทึก CheckInLog พร้อม checkout_time
  - Status model อัปเดต: `status='available'`, `current_user_name=None`, `start_time=None`
  - หน้า Dashboard รีเฟรช
  - เครื่อง PC-01 แสดงสถานะ "ว่าง"

**Actual**: ✅ Pass

---

### Test Case 2: ส่งซ่อมจากเครื่องที่ใช้งานอยู่

**Steps**:
1. เข้าหน้า Dashboard
2. มีเครื่อง PC-01 สถานะ `in_use` (มีผู้ใช้งาน)
3. คลิกที่เครื่อง PC-01
4. เลือกปุ่ม "ส่งซ่อม" (สีเทา)
5. กด "ยืนยันส่งซ่อม"

**Expected**:
- แสดงข้อความ "คุณต้องการส่งเครื่อง PC-01 เข้าซ่อม ใช่หรือไม่?"
- แสดงหมายเหตุ "ระบบจะทำการ Checkout ผู้ใช้งานปัจจุบันออกอัตโนมัติ"
- หลังกดยืนยัน:
  - เครื่อง PC-01 เปลี่ยนสถานะเป็น `maintenance`
  - บันทึก CheckInLog พร้อม checkout_time
  - Status model อัปเดต: `status='maintenance'`, `current_user_name=None`, `start_time=None`
  - หน้า Dashboard รีเฟรช
  - เครื่อง PC-01 แสดงสถานะ "ส่งซ่อม" (สีเทา)

**Actual**: ✅ Pass

---

### Test Case 3: เปิดใช้งานเครื่องซ่อม

**Steps**:
1. เข้าหน้า Dashboard
2. มีเครื่อง PC-02 สถานะ `maintenance`
3. คลิกที่เครื่อง PC-02
4. กด "เปิดใช้งาน"

**Expected**:
- แสดงข้อความ "คุณต้องการ 'เปิดใช้งาน (Available)' ใช่หรือไม่?"
- หลังกดยืนยัน:
  - เครื่อง PC-02 เปลี่ยนสถานะเป็น `available`
  - Status model อัปเดต: `status='available'`
  - หน้า Dashboard รีเฟรช
  - เครื่อง PC-02 แสดงสถานะ "ว่าง" (สีเขียว)

**Actual**: ✅ Pass

---

### Test Case 4: ยกเลิกการดำเนินการ

**Steps**:
1. เข้าหน้า Dashboard
2. มีเครื่อง PC-01 สถานะ `in_use`
3. คลิกที่เครื่อง PC-01
4. แสดงเมนู "Checkout" และ "ส่งซ่อม"
5. กดปุ่ม "ยกเลิก"

**Expected**:
- Dialog ปิด
- ไม่มีการเปลี่ยนแปลงสถานะ
- เครื่อง PC-01 ยังคงสถานะ `in_use`

**Actual**: ✅ Pass

---

### Test Case 5: เครื่องว่าง (Available) - พฤติกรรมเดิม

**Steps**:
1. เข้าหน้า Dashboard
2. มีเครื่อง PC-03 สถานะ `available`
3. คลิกที่เครื่อง PC-03
4. กรอกรหัสเจ้าหน้าที่ "12345"
5. กด "ยืนยัน"

**Expected**:
- แสดง Dialog "กรุณากรอกรหัสเจ้าหน้าที่"
- หลังกดยืนยัน:
  - เครื่อง PC-03 เปลี่ยนสถานะเป็น `in_use`
  - Status model อัปเดต: `status='in_use'`, `current_user_id='12345'`, `current_user_type='staff'`
  - หน้า Dashboard รีเฟรช

**Actual**: ✅ Pass

---

## 💡 Technical Implementation

### SweetAlert2 Multi-Button Dialog

```javascript
Swal.fire({
    showDenyButton: true,      // เพิ่มปุ่ม Deny
    showCancelButton: true,    // เพิ่มปุ่ม Cancel
    confirmButtonText: '...',  // ปุ่มหลัก
    denyButtonText: '...',     // ปุ่มรอง
    cancelButtonText: '...'    // ปุ่มยกเลิก
}).then((result) => {
    if (result.isConfirmed) {
        // ผู้ใช้กด Confirm
    } else if (result.isDenied) {
        // ผู้ใช้กด Deny
    }
    // ถ้ากด Cancel จะไม่ทำอะไร
});
```

### API Call Pattern

```javascript
updateComputerStatus(pcId, newStatus, forceCheckout, staffId)
    ↓
fetch(`/api/update-computer/${pcId}/`, {
    method: 'POST',
    body: {
        status: newStatus,
        force_checkout: forceCheckout,
        staff_id: staffId
    }
})
    ↓
Backend (update_computer view)
    ↓
if force_checkout == 'true' และมีผู้ใช้งาน:
    - บันทึก checkout_time ใน CheckInLog
    - อัปเดต Status model
    ↓
อัปเดต status เป็นค่าใหม่
    ↓
return JsonResponse({'success': True})
```

---

## 🔗 Backend Integration

### API Endpoint: `/api/update-computer/<pc_id>/`

**Function**: `core.views.update_computer()`

**Parameters**:
- `status`: สถานะใหม่ (`'available'`, `'in_use'`, `'maintenance'`)
- `force_checkout`: `'true'` หรือ `'false'` (string)
- `staff_id`: รหัสเจ้าหน้าที่ (optional, สำหรับ available → in_use)

**Logic**:
1. ถ้า `force_checkout == 'true'`:
   - หา CheckInLog ที่ยังไม่มี checkout_time
   - บันทึก checkout_time = now
   - คำนวณ duration_minutes
2. อัปเดต Status model:
   - `status` = ค่าใหม่
   - ถ้า status = `'available'` หรือ `'maintenance'`:
     - `current_user_name = None`
     - `current_user_id = None`
     - `current_user_type = None`
     - `start_time = None`
     - `software = None`
3. ถ้า status = `'in_use'` และมี `staff_id`:
   - `current_user_id = staff_id`
   - `current_user_type = 'staff'`
   - `current_user_name = f'เจ้าหน้าที่ {staff_id}'`
   - `start_time = now`

---

## 📊 State Transition Table

| สถานะปัจจุบัน | Action | สถานะใหม่ | Force Checkout | Staff ID |
|---------------|--------|-----------|----------------|----------|
| `available` | คลิก | `in_use` | ❌ | ✅ Required |
| `in_use` → Checkout | คลิก | `available` | ✅ | ❌ |
| `in_use` → ส่งซ่อม | คลิก | `maintenance` | ✅ | ❌ |
| `maintenance` | คลิก | `available` | ❌ | ❌ |
| `disabled` | คลิก | `available` | ❌ | ❌ |

---

## 🚀 Benefits

### 1. **ความยืดหยุ่นในการจัดการ**
- Admin สามารถ checkout ผู้ใช้งานออกได้ทันที (กรณีฉุกเฉิน)
- สามารถส่งซ่อมเครื่องที่กำลังใช้งานได้ (force checkout อัตโนมัติ)

### 2. **UX ที่ดีขึ้น**
- เมนูชัดเจน ง่ายต่อการตัดสินใจ
- แสดงคำเตือนก่อนทำการ force checkout
- ปุ่มสีต่างกันชัดเจน (แดง=checkout, เทา=ซ่อม)

### 3. **ป้องกันข้อผิดพลาด**
- ต้องยืนยัน 2 ครั้ง (เลือกเมนู → ยืนยัน)
- แสดงหมายเหตุชัดเจนว่าจะมี force checkout

### 4. **รองรับ Legacy Status**
- ยังคงรองรับ `disabled` status (เครื่องเก่า)
- สามารถเปิดใช้งานได้ทันที

---

## 🔗 Related Files

- **Template**: [templates/manager/dashboard.html](templates/manager/dashboard.html)
- **View**: [core/views.py](core/views.py) - `update_computer()`
- **API URL**: [core/urls.py](core/urls.py) - `/api/update-computer/<pc_id>/`
- **CSS**: [static/css/admin.css](static/css/admin.css) - `.maintenance` class

---

## 📝 Related Updates

This update completes the dashboard management system:
1. **CSV Export Filtering** - [CSV_EXPORT_FILTER_UPDATE.md](CSV_EXPORT_FILTER_UPDATE.md)
2. **Manage Page Refactor** - [MANAGE_PAGE_REFACTOR.md](MANAGE_PAGE_REFACTOR.md)
3. **Check-in Redirect** - Updated in [core/views.py:208](core/views.py#L208)
4. **Index Page Enhancement** - [INDEX_PAGE_ENHANCEMENT.md](INDEX_PAGE_ENHANCEMENT.md)
5. **Dashboard In-Use Management** - This document

---

**Last Updated**: 2025-12-01

**Status**: ✅ Completed

**Tested**: Docker environment

**Compatibility**: Works seamlessly with Status model and all related features
