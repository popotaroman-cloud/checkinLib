# 🏠 Index (Kiosk) Page Enhancement - Lab Check-in System

## Overview

อัปเดตหน้าหลัก (Kiosk) ให้เชื่อมต่อโดยตรงกับ **Status model** และแสดงข้อมูลการใช้งานแบบละเอียด พร้อมตัวนับเวลาแบบเรียลไทม์

---

## ✨ Features ที่เพิ่มเข้ามา

### 1. **เชื่อมต่อกับ Status Model โดยตรง**
- ใช้ `pc.status_info.status` แทน `pc.status` (ที่ไม่มีอยู่จริง)
- ใช้ค่า `'in_use'` แทน `'occupied'` ตาม Status model ที่แท้จริง
- ดึงข้อมูลผู้ใช้, ซอฟต์แวร์, เวลาเริ่มต้น จาก Status model

### 2. **แสดงสถานะการเช็คอิน**
- **Available**: แสดงปุ่ม Check-in พร้อมลิงก์ไปหน้า checkin
- **In Use**: แสดงข้อมูลผู้ใช้, ซอฟต์แวร์, ระยะเวลา พร้อมปุ่ม Check-out
- **Maintenance**: แสดงสถานะงดให้บริการ

### 3. **ปุ่ม Check-in และ Check-out**
- ปุ่มขนาดใหญ่สีเขียวสำหรับ Check-in
- ปุ่มขนาดใหญ่สีแดงสำหรับ Check-out
- มีไอคอนและข้อความชัดเจน

### 4. **แสดงข้อมูลเมื่อมีการใช้งาน**
เมื่อเครื่องมีสถานะ `in_use` จะแสดง:
- **ชื่อผู้ใช้**: จาก `status.current_user_name`
- **ประเภทผู้ใช้**: นักศึกษา/เจ้าหน้าที่/บุคคลภายนอก
- **ซอฟต์แวร์**: จาก `status.software` (ถ้ามี)
- **ระยะเวลา**: คำนวณจาก `status.start_time` ถึงปัจจุบัน

### 5. **ตัวนับเวลาแบบเรียลไทม์**
- คำนวณระยะเวลาใช้งานเป็นนาที
- อัปเดตทุก 60 วินาที (1 นาที) โดยอัตโนมัติ
- ไม่ต้องรีเฟรชหน้า

---

## 📁 Files Modified

### 1. `core/views.py` - index() function (Lines 115-141)

**Changes**:

#### 1.1 เพิ่มการดึงข้อมูล Status
```python
def index(request):
    """หน้าจอ Idle แสดง QR Code พร้อมข้อมูลสถานะ"""
    pc_id = request.GET.get('pc_id', 'PC-01')

    # สร้างข้อมูลเครื่องจำลองถ้ายังไม่มีใน DB (เพื่อความสะดวกตอนรันครั้งแรก)
    pc, created = Computer.objects.get_or_create(pc_id=pc_id)

    # สร้าง Status ถ้ายังไม่มี
    if created:
        Status.objects.create(computer=pc, status='available')

    # ดึงข้อมูล status โดยตรง
    status = pc.status_info

    # คำนวณระยะเวลาใช้งาน (ถ้ากำลังใช้งานอยู่)
    duration_minutes = 0
    if status.status == 'in_use' and status.start_time:
        now = timezone.now()
        diff = now - status.start_time
        duration_minutes = int(diff.total_seconds() / 60)

    context = {
        'pc': pc,
        'status': status,
        'duration_minutes': duration_minutes
    }
    return render(request, 'index.html', context)
```

**Key Points**:
- ดึง `status` จาก `pc.status_info` (OneToOneField)
- คำนวณ `duration_minutes` จาก `start_time` ถึงปัจจุบัน
- ส่งทั้ง `pc`, `status`, `duration_minutes` ไปยัง template

---

### 2. `templates/index.html` - MAJOR REFACTOR

**Location**: Lines 23-101, 111-124

**Changes**:

#### 2.1 เปลี่ยนจาก `pc.status` เป็น `status.status`

**Before**:
```html
{% if pc.status == 'available' %}
    ...
{% elif pc.status == 'occupied' %}
    ...
{% endif %}
```

**After**:
```html
{% if status.status == 'available' %}
    ...
{% elif status.status == 'in_use' %}
    ...
{% endif %}
```

#### 2.2 เพิ่มปุ่ม Check-in (เมื่อเครื่องว่าง)

```html
{% if status.status == 'available' %}
    <!-- QR Code -->
    <a href="{% url 'checkin' pc.pc_id %}" style="text-decoration: none;">
        <div class="qr-container" title="คลิกเพื่อจำลองการสแกน (เข้าใช้งาน)">
            <i class="fas fa-qrcode"></i>
        </div>
    </a>
    <h2 style="color: var(--success);"><i class="fas fa-check-circle"></i> เครื่องว่าง (Available)</h2>
    <p class="mt-2">สแกน QR Code เพื่อ <b>เข้าใช้งาน</b></p>

    <!-- ปุ่ม Check-in -->
    <div style="margin-top: 30px;">
        <a href="{% url 'checkin' pc.pc_id %}" class="btn" style="background-color: var(--success); color: white; padding: 15px 40px; font-size: 1.2rem; text-decoration: none; border-radius: 8px; display: inline-block;">
            <i class="fas fa-sign-in-alt"></i> เข้าใช้งาน (Check-in)
        </a>
    </div>
{% endif %}
```

#### 2.3 แสดงข้อมูลการใช้งานและปุ่ม Check-out (เมื่อมีคนใช้)

```html
{% elif status.status == 'in_use' %}
    <!-- QR Code -->
    <a href="{% url 'checkout' pc.pc_id %}" style="text-decoration: none;">
        <div class="qr-container" style="border-color: var(--danger); background-color: #fff5f5;" title="คลิกเพื่อจำลองการสแกน (เลิกใช้งาน)">
            <i class="fas fa-sign-out-alt" style="color: var(--danger);"></i>
        </div>
    </a>
    <h2 style="color: var(--danger);"><i class="fas fa-user-clock"></i> ใช้งานอยู่โดย: {{ status.current_user_name }}</h2>

    <!-- แสดงข้อมูลการใช้งาน -->
    <div style="margin-top: 20px; background-color: #f8f9fa; padding: 20px; border-radius: 8px; border: 1px solid #dee2e6;">
        <div style="display: grid; gap: 15px; text-align: left; max-width: 500px; margin: 0 auto;">
            <!-- ชื่อผู้ใช้ -->
            <div>
                <strong style="color: var(--text-muted);">
                    <i class="fas fa-user"></i> ผู้ใช้งาน:
                </strong>
                <span style="color: var(--text-color); font-size: 1.1rem;">{{ status.current_user_name }}</span>
            </div>

            <!-- ประเภทผู้ใช้ -->
            <div>
                <strong style="color: var(--text-muted);">
                    <i class="fas fa-id-card"></i> ประเภท:
                </strong>
                <span style="color: var(--text-color);">{{ status.get_current_user_type_display }}</span>
            </div>

            <!-- ซอฟต์แวร์ -->
            {% if status.software %}
            <div>
                <strong style="color: var(--text-muted);">
                    <i class="fas fa-box"></i> ซอฟต์แวร์:
                </strong>
                <span style="color: var(--text-color);">{{ status.software }}</span>
            </div>
            {% endif %}

            <!-- ระยะเวลาใช้งาน -->
            <div>
                <strong style="color: var(--text-muted);">
                    <i class="fas fa-clock"></i> ระยะเวลา:
                </strong>
                <span style="color: var(--primary-color); font-size: 1.3rem; font-weight: bold;">
                    <span id="duration-counter">{{ duration_minutes }}</span> นาที
                </span>
            </div>
        </div>
    </div>

    <!-- ปุ่ม Checkout -->
    <div style="margin-top: 30px;">
        <a href="{% url 'checkout' pc.pc_id %}" class="btn" style="background-color: var(--danger); color: white; padding: 15px 40px; font-size: 1.2rem; text-decoration: none; border-radius: 8px; display: inline-block;">
            <i class="fas fa-sign-out-alt"></i> ลงชื่อออก (Check-out)
        </a>
    </div>
{% endif %}
```

#### 2.4 เพิ่ม JavaScript สำหรับตัวนับเวลา

```html
{% block extra_js %}
{% if status.status == 'in_use' %}
<script>
    // อัปเดตตัวนับเวลาทุกนาที
    let durationMinutes = {{ duration_minutes }};
    const counterElement = document.getElementById('duration-counter');

    setInterval(() => {
        durationMinutes++;
        counterElement.textContent = durationMinutes;
    }, 60000); // อัปเดตทุก 60 วินาที (1 นาที)
</script>
{% endif %}
{% endblock %}
```

**Key Points**:
- ใช้ `setInterval()` เพื่ออัปเดตทุก 60 วินาที
- เริ่มต้นด้วยค่า `duration_minutes` จาก backend
- อัปเดต DOM element `#duration-counter` โดยตรง

---

## 🎯 How It Works

### Flow Diagram

```
User เข้าหน้า Kiosk (/?pc_id=PC-01)
    ↓
index() view ดึงข้อมูล Computer และ Status
    ↓
คำนวณ duration_minutes (ถ้ากำลังใช้งาน)
    ↓
ส่งข้อมูลไป template: pc, status, duration_minutes
    ↓
Template ตรวจสอบ status.status
    ↓
┌─────────────────────────────────┬──────────────────────────────┬─────────────────┐
│  'available'                    │  'in_use'                    │  'maintenance'  │
├─────────────────────────────────┼──────────────────────────────┼─────────────────┤
│  - แสดง QR Code สีเขียว         │  - แสดง QR Code สีแดง        │  - แสดงไอคอน    │
│  - แสดงสถานะ "เครื่องว่าง"      │  - แสดงชื่อผู้ใช้             │    เครื่องมือ   │
│  - แสดงปุ่ม Check-in            │  - แสดงประเภทผู้ใช้           │  - งดให้บริการ  │
│                                 │  - แสดงซอฟต์แวร์              │                 │
│                                 │  - แสดงตัวนับเวลา (อัปเดตทุกนาที) │                 │
│                                 │  - แสดงปุ่ม Check-out         │                 │
└─────────────────────────────────┴──────────────────────────────┴─────────────────┘
```

---

## 🎨 UI Changes

### Before
```
┌─────────────────────────────────┐
│  🖥️                             │
│  ระบบเช็คอินห้องปฏิบัติการ      │
│  หมายเลขเครื่อง: PC-01         │
│                                 │
│  [QR Code]                      │
│                                 │
│  ✅ เครื่องว่าง (Available)     │
│  สแกน QR Code เพื่อ เข้าใช้งาน  │
│                                 │
└─────────────────────────────────┘
```

### After (Available)
```
┌─────────────────────────────────┐
│  🖥️                             │
│  ระบบเช็คอินห้องปฏิบัติการ      │
│  หมายเลขเครื่อง: PC-01         │
│                                 │
│  [QR Code]                      │
│                                 │
│  ✅ เครื่องว่าง (Available)     │
│  สแกน QR Code เพื่อ เข้าใช้งาน  │
│                                 │
│  ┌───────────────────────────┐  │
│  │  ⮞ เข้าใช้งาน (Check-in)  │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

### After (In Use)
```
┌──────────────────────────────────────┐
│  🖥️                                  │
│  ระบบเช็คอินห้องปฏิบัติการ           │
│  หมายเลขเครื่อง: PC-01              │
│                                      │
│  [QR Code - สีแดง]                  │
│                                      │
│  🕐 ใช้งานอยู่โดย: นายสมชาย ใจดี     │
│                                      │
│  ┌────────────────────────────────┐  │
│  │  👤 ผู้ใช้งาน: นายสมชาย ใจดี    │  │
│  │  🆔 ประเภท: นักศึกษา             │  │
│  │  📦 ซอฟต์แวร์: AutoCAD 2022     │  │
│  │  🕐 ระยะเวลา: 45 นาที ⟲         │  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │  ⮜ ลงชื่อออก (Check-out)        │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

**Benefits**:
- ข้อมูลครบถ้วนทุกอย่างที่จำเป็น
- ปุ่มชัดเจน ง่ายต่อการใช้งาน
- ตัวนับเวลาอัปเดตเรียลไทม์
- ไม่ต้องรีเฟรชหน้าเพื่อดูเวลา

---

## 🧪 Test Cases

### Test Case 1: หน้าจอเครื่องว่าง (Available)

**Steps**:
1. เข้าหน้า `http://localhost:8000/?pc_id=PC-01`
2. เครื่อง PC-01 มีสถานะ `available`

**Expected**:
- แสดงสถานะ "เครื่องว่าง (Available)" สีเขียว
- แสดง QR Code สีเขียว
- แสดงปุ่ม "เข้าใช้งาน (Check-in)" สีเขียว
- คลิกปุ่มแล้วไปหน้า checkin

**Actual**: ✅ Pass

---

### Test Case 2: หน้าจอเครื่องใช้งานอยู่ (In Use)

**Steps**:
1. เช็คอินเครื่อง PC-01 ด้วยผู้ใช้ใดๆ
2. เข้าหน้า `http://localhost:8000/?pc_id=PC-01`

**Expected**:
- แสดงสถานะ "ใช้งานอยู่โดย: [ชื่อผู้ใช้]" สีแดง
- แสดง QR Code สีแดง
- แสดงกล่องข้อมูล:
  - ชื่อผู้ใช้
  - ประเภทผู้ใช้
  - ซอฟต์แวร์ (ถ้ามี)
  - ระยะเวลา X นาที
- แสดงปุ่ม "ลงชื่อออก (Check-out)" สีแดง
- คลิกปุ่มแล้วไปหน้า checkout

**Actual**: ✅ Pass

---

### Test Case 3: ตัวนับเวลาอัปเดตเรียลไทม์

**Steps**:
1. เช็คอินเครื่อง PC-01
2. เข้าหน้า `http://localhost:8000/?pc_id=PC-01`
3. สังเกตตัวนับเวลา
4. รอ 1 นาที

**Expected**:
- ตัวนับเวลาเพิ่มขึ้น 1 นาทีโดยอัตโนมัติ
- ไม่มีการรีเฟรชหน้า
- ตัวเลขอัปเดตอย่างนุ่มนวล

**Actual**: ✅ Pass

---

### Test Case 4: เครื่องงดให้บริการ (Maintenance)

**Steps**:
1. เปลี่ยนสถานะ PC-01 เป็น `maintenance`
2. เข้าหน้า `http://localhost:8000/?pc_id=PC-01`

**Expected**:
- แสดงไอคอนเครื่องมือสีเทา
- แสดงข้อความ "งดให้บริการ (Maintenance)"
- แสดงข้อความ "ขออภัยในความไม่สะดวก"
- ไม่มีปุ่มใดๆ

**Actual**: ✅ Pass

---

### Test Case 5: ข้อมูลซอฟต์แวร์แสดงถูกต้อง

**Steps**:
1. เช็คอินเครื่อง PC-01 และเลือกซอฟต์แวร์ "AutoCAD 2022"
2. เข้าหน้า `http://localhost:8000/?pc_id=PC-01`

**Expected**:
- ในกล่องข้อมูลแสดง "ซอฟต์แวร์: AutoCAD 2022"

**Actual**: ✅ Pass

---

### Test Case 6: ไม่มีซอฟต์แวร์ (None)

**Steps**:
1. เช็คอินเครื่อง PC-01 โดยไม่เลือกซอฟต์แวร์
2. เข้าหน้า `http://localhost:8000/?pc_id=PC-01`

**Expected**:
- ในกล่องข้อมูลไม่มีแถว "ซอฟต์แวร์"
- แสดงเฉพาะผู้ใช้, ประเภท, ระยะเวลา

**Actual**: ✅ Pass

---

## 🔄 Integration with Other Features

### 1. ทำงานร่วมกับ Status Model
- ดึงข้อมูลจาก `Status.current_user_name`
- ดึงข้อมูลจาก `Status.current_user_type`
- ดึงข้อมูลจาก `Status.software`
- ดึงข้อมูลจาก `Status.start_time`
- ใช้ `Status.status` เป็นเงื่อนไขแสดงผล

### 2. ทำงานร่วมกับ Check-in Flow
- ปุ่ม Check-in ลิงก์ไปหน้า `{% url 'checkin' pc.pc_id %}`
- หลังจาก check-in สำเร็จ จะ redirect กลับมาหน้า checkout (ตาม update ก่อนหน้า)

### 3. ทำงานร่วมกับ Check-out Flow
- ปุ่ม Check-out ลิงก์ไปหน้า `{% url 'checkout' pc.pc_id %}`
- QR Code ก็ลิงก์ไปหน้า checkout เช่นกัน

---

## 💡 Technical Implementation

### Duration Calculation Logic

```python
# คำนวณระยะเวลาใช้งาน (ถ้ากำลังใช้งานอยู่)
duration_minutes = 0
if status.status == 'in_use' and status.start_time:
    now = timezone.now()
    diff = now - status.start_time
    duration_minutes = int(diff.total_seconds() / 60)
```

**Explanation**:
1. เช็คว่าสถานะเป็น `'in_use'` และมี `start_time`
2. ใช้ `timezone.now()` เพื่อดึงเวลาปัจจุบัน (timezone-aware)
3. คำนวณความต่าง (`timedelta`)
4. แปลงเป็นวินาทีแล้วหารด้วย 60 เพื่อได้นาที
5. ใช้ `int()` เพื่อตัดทศนิยม

### Real-time Counter Logic

```javascript
let durationMinutes = {{ duration_minutes }};
const counterElement = document.getElementById('duration-counter');

setInterval(() => {
    durationMinutes++;
    counterElement.textContent = durationMinutes;
}, 60000);
```

**Explanation**:
1. เริ่มต้นด้วยค่าจาก backend (คำนวณแล้ว)
2. ใช้ `setInterval()` เพื่อรันทุก 60,000 มิลลิวินาที (1 นาที)
3. เพิ่มค่า `durationMinutes` ขึ้น 1
4. อัปเดต DOM element `#duration-counter`

**Why 60 seconds instead of real-time?**
- ประหยัด CPU/battery
- ความแม่นยำระดับนาทีเพียงพอ
- UX ดีกว่า (ไม่มีตัวเลขกระโดด)

---

## 📊 Data Flow

### View → Template

```python
context = {
    'pc': pc,                      # Computer object
    'status': status,              # Status object
    'duration_minutes': 45         # int (คำนวณแล้ว)
}
```

### Template Usage

```html
{{ pc.pc_id }}                     → "PC-01"
{{ status.status }}                → "in_use"
{{ status.current_user_name }}     → "นายสมชาย ใจดี"
{{ status.get_current_user_type_display }} → "นักศึกษา"
{{ status.software }}              → "AutoCAD 2022"
{{ duration_minutes }}             → 45
```

---

## 🚀 Benefits

### 1. **ข้อมูลสมบูรณ์**
- ผู้ใช้เห็นข้อมูลครบถ้วนทันทีโดยไม่ต้อง check-out
- รู้ว่ากำลังใช้งานมานานเท่าไหร่

### 2. **UX ที่ดีขึ้น**
- มีปุ่มชัดเจนสำหรับ Check-in และ Check-out
- ไม่ต้องพึ่ง QR Code อย่างเดียว
- ข้อมูลจัดวางอย่างเป็นระเบียบ

### 3. **Real-time Information**
- ตัวนับเวลาอัปเดตอัตโนมัติ
- ไม่ต้องรีเฟรชหน้า

### 4. **ความถูกต้อง**
- ใช้ข้อมูลจาก Status model โดยตรง
- ไม่มีความเสี่ยงที่ข้อมูลไม่ตรงกัน

---

## 🔗 Related Files

- **View**: [core/views.py:115-141](core/views.py#L115-L141)
- **Template**: [templates/index.html](templates/index.html)
- **Model**: [core/models.py](core/models.py) - Status, Computer
- **URL**: [core/urls.py](core/urls.py) - `path('', views.index, name='index')`

---

## 📝 Related Updates

This update completes a series of enhancements:
1. **CSV Export Filtering** - [CSV_EXPORT_FILTER_UPDATE.md](CSV_EXPORT_FILTER_UPDATE.md)
2. **Manage Page Refactor** - [MANAGE_PAGE_REFACTOR.md](MANAGE_PAGE_REFACTOR.md)
3. **Check-in Redirect** - Updated in [core/views.py:208](core/views.py#L208)
4. **Index Page Enhancement** - This document

---

**Last Updated**: 2025-12-01

**Status**: ✅ Completed

**Tested**: Docker environment

**Compatibility**: Works seamlessly with Status model and all related features
