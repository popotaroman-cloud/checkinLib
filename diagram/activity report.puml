@startuml
|Admin (ผู้ดูแลระบบ)|
start
:เข้าสู่เมนู "รายงานและสถิติ";
:ระบุเงื่อนไข (Filter Criteria)
- ช่วงเวลา (Start - End Date)
- ประเภทผู้ใช้ / คณะ / ชั้นปี;
:กดปุ่ม "แสดงรายงาน";

|System (ระบบ)|
:Query ข้อมูลจากฐานข้อมูล
(Usage Log + User Info + Feedback);
:ประมวลผลสถิติและจัดกลุ่มข้อมูล;
:สร้างกราฟ (Bar/Pie Chart) แสดงผลหน้าเว็บ;

|Admin (ผู้ดูแลระบบ)|
:ดูผลลัพธ์กราฟ;
if (ต้องการไฟล์ข้อมูล?) then (Yes)
    :กดปุ่ม "Export CSV";

    |System (ระบบ)|
    :แปลงข้อมูลที่ Filter แล้วเป็น format CSV;
    :ส่งไฟล์ให้ Browser ดาวน์โหลด;
else (No)
    :จบการทำงาน;
endif

stop
@enduml